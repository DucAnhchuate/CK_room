<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CK_room"
        tools:targetApi="31">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.CK_room">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Sign_up"></activity>
        <activity android:name=".Admin_page"></activity>
        <activity android:name=".User_page"></activity>
        <activity android:name=".Add_Train"></activity>
        <activity android:name=".Add_Train_Status"></activity>
        <activity android:name=".Add_route"></activity>
        <activity android:name=".Add_station"></activity>
        <activity android:name=".Manage_train"></activity>
        <activity android:name=".Admin_manage_profile"></activity>
        <activity android:name=".Search"></activity>
        <activity android:name=".User_edit_profile"></activity>
        <activity android:name=".User_choose_seat"></activity>
        <activity android:name=".Schedule"></activity>
        <activity android:name=".User_buy_ticket"></activity>


    </application>

</manifest>